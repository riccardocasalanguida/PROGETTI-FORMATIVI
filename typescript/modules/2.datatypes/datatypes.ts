let stringa: string = "bruno"; //stringhe
let numero: number = 123; // numeri
let booleano: boolean = true; //booleani
let oggetto: object = {}; // oggetto
let array: string[] = []; // array di stringhe
let union: string | number | boolean; // union di tipi
let any: any = "fabio"; // qualunque dato
let unk: unknown = 12; // dato non conosciuto
unk = true;
let vo: void; // dato senza ritorno
let nev: never; // dato che non tornerà mai
